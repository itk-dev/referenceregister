<p>{{ 'Click the link to log in to {site_name}.'|trans({site_name: site_name}) }}</p>

<p><a href="{{ login_link_details.url }}">{{ 'Log in'|trans }}</a></p>

<p>{{ 'The link is valid for {minutes} minutes and can only be used once.'|trans({minutes: expires_in_minutes}) }}</p>

<p>{{ 'If you did not request this link, you can safely ignore this message.'|trans }}</p>
